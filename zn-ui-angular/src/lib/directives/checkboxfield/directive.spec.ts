import { znCheckboxFieldDirective } from './directive';

describe('znCheckboxFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znCheckboxFieldDirective(null);
    expect(directive).toBeTruthy();
  });
});
