import { znRadioGroupDirective } from './directive';

describe('znRadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new znRadioGroupDirective(null);
    expect(directive).toBeTruthy();
  });
});
