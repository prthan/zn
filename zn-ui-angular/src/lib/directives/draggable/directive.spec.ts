import { znDraggableDirective } from './directive';

describe('znDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new znDraggableDirective(null);
    expect(directive).toBeTruthy();
  });
});
