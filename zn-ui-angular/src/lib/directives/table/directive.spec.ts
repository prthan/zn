import { znTableDirective } from './directive';

describe('znTableDirective', () => {
  it('should create an instance', () => {
    const directive = new znTableDirective(null);
    expect(directive).toBeTruthy();
  });
});
