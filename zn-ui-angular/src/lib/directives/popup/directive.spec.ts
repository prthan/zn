import { znPopupDirective } from './directive';

describe('znPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new znPopupDirective(null);
    expect(directive).toBeTruthy();
  });
});
