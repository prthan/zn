import { znDropdownFieldDirective } from './directive';

describe('znDropdownFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znDropdownFieldDirective(null);
    expect(directive).toBeTruthy();
  });
});
