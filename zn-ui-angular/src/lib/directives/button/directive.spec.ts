import { znButtonDirective } from './directive';

describe('znCheckboxFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znButtonDirective(null);
    expect(directive).toBeTruthy();
  });
});
