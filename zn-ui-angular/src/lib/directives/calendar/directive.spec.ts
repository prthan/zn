import { znCalendarDirective } from './directive';

describe('znTextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znCalendarDirective(null);
    expect(directive).toBeTruthy();
  });
});
