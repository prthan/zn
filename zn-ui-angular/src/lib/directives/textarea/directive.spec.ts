import { znTextAreaDirective } from './directive';

describe('znTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new znTextAreaDirective(null);
    expect(directive).toBeTruthy();
  });
});
