import { znTextFieldDirective } from './directive';

describe('znTextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znTextFieldDirective(null);
    expect(directive).toBeTruthy();
  });
});
