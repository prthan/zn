import { znDateFieldDirective } from './directive';

describe('znDateFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new znDateFieldDirective(null);
    expect(directive).toBeTruthy();
  });
});
