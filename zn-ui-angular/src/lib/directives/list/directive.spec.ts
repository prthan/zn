import { znListDirective } from './directive';

describe('znListDirective', () => {
  it('should create an instance', () => {
    const directive = new znListDirective(null);
    expect(directive).toBeTruthy();
  });
});
